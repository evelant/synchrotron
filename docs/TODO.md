High level misc todos

- Fix @effect/sql-sqlite-react-native, uses old v7.1.0 of op-sqlite which doesn't build on latest expo
- Update example react native app to use sql-sqlite-react-native instead of straight op-sqlite
- Implement server retention/compaction of the action log (see `docs/server-retention-compaction.md`)
