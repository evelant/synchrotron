ADMIN_DATABASE_URL='postgresql://postgres:password@localhost:56321/electric'
DATABASE_URL='postgresql://synchrotron_app:password@localhost:56321/electric'
PROJECT_NAME='synchrotron-examples-backend'

# Optional: enable JWT auth for Synchrotron RPC (HS256, Supabase-compatible claim defaults).
# When set, the RPC server requires `Authorization: Bearer <jwt>` and derives `user_id` from `sub`.
# SYNC_JWT_SECRET='supersecretvalue-supersecretvalue'
# SYNC_JWT_AUD='authenticated'
